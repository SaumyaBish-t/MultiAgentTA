"""
meta_analysis
=============

L11 — pure-analytical layer that measures what's actually working.
Reads from every other layer, never modifies. Disabled when
``settings.meta_analysis_enabled`` is False.
"""
